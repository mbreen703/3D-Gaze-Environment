

/*
default export class Utils {
    static utilMethod = function(e) => {
        var sceneData = e;
        return (sceneData);
    }
}


export const helper = (e) => {
    var sceneData = e.data;
    var corrFormat = JSON.stringify(sceneData);
    return corrFormat;
}

var received = function(e) {

    var sceneData = e.data;
    return sceneData;
}

exports.received = received;

onmessage = function(e) {

    var sceneData = e.data;
    //Testing
    //console.log(sceneData.json);
    //console.log(sceneData.loader);
    received(sceneData);

};

export function received(sceneData) {
    //const sceneData = e.data;
    return sceneData;
}

const messenger = {
    
    received: function(e) {
        
        const sceneData = e.data;
        return sceneData;
*/


